name: Deployement to staging
on:
    push:
        branches:
            - main
jobs:
    # test: 
    #     runs-on: ubuntu-latest
    #     steps:
    #         - name: checkout code
    #           uses: actions/checkout@v6
    #         - name: install dependencies
    #           run: npm i
    #         - name: run test
    #           run: npm run test
    # build-and-push:
    #     runs-on: ubuntu-latest
    #     needs: test
    #     steps:
    #         - name: checkout code
    #           uses: actions/checkout@v6
    #         - name: build docker image
    #           run: docker build -t oumaymasfaxi/backend-session16 .
    #         - name: tag image by version
    #           run: docker tag oumaymasfaxi/backend-session16 oumaymasfaxi/backend-session16:v1
    #         - name: list images
    #           run: docker images 
    #         - name: docker login
    #           uses: docker/login-action@v4
    #           with:
    #             username: ${{ vars.DOCKERHUB_USERNAME }}
    #             password: ${{ secrets.DOCKERHUB_TOKEN }}
    #         - name: push image to dockerhub
    #           run: docker push oumaymasfaxi/backend-session16:v1
    #         - name: push image to dockerhub
    #           run: docker push oumaymasfaxi/backend-session16
    Deploy:
      runs-on: ubuntu-latest
      #needs: build-and-push
      steps:
        - uses: appleboy/ssh-action@v1
          with:
            host: ${{ secrets.VPS_HOST }}
            username: ${{ secrets.VPS_USERNAME }}
            password: ${{ secrets.VPS_PASSWORD }}
            script: whoami
        