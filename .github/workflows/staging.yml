name: Deployement to staging
on:
    push:
        branches:
            - main
jobs:
    test: 
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v6
            - name: install dependencies
              run: npm i
            - name: run test
              run: npm run test